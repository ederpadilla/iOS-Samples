
# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'IOS Samples' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IOS Samples
   pod 'GoogleMaps'
   pod 'GooglePlaces'
   pod 'Firebase/Core'
   pod 'Firebase/Auth'
   pod 'Firebase/Database'
   pod 'Firebase/Storage'
  target 'IOS SamplesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'IOS SamplesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
